import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.corhuila.registro',
  appName: 'registroEstudiantes',
  webDir: 'www'
};

export default config;
